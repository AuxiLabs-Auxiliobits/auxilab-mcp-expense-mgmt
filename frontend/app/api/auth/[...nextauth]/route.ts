// REFERENCE SCAFFOLD ONLY — see README.md.
// NextAuth (Auth.js v5) catch-all route. The handlers are configured in lib/auth.ts
// with the Microsoft Entra External ID provider (SCOPING §10/§13).

export { GET, POST } from "@/lib/auth";
