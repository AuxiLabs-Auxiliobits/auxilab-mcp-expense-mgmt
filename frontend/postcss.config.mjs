/** @type {import('postcss-load-config').Config} */
// REFERENCE SCAFFOLD ONLY — see README.md.
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
