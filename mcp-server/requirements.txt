# MCP Server — pinned production dependencies
# Python 3.12+ required.
#
# Install:
#   pip install -r requirements.txt
#
# Or install the package (recommended):
#   pip install -e ./mcp-server
#
# The MCP server also depends on expense-core (the sibling workspace package).
# Install it first:
#   pip install -e ./core-engine

# MCP SDK
mcp==1.27.2

# Validation
pydantic==2.13.4
pydantic-core==2.46.4

# HTTP client (authenticated calls to the FastAPI backend)
httpx==0.28.1
httpcore==1.0.9
anyio==4.13.0
h11==0.16.0

# Local workspace package — install from monorepo root
# pip install -e ./core-engine -e ./mcp-server
