# Workers — pinned production dependencies (offline mode, no Azure)
# Python 3.12+ required.
#
# Install (offline / dev):
#   pip install -r requirements.txt
#
# Install (with Azure integrations):
#   pip install -r requirements.txt ".[azure]"
#
# Local workspace packages must be installed first:
#   pip install -e ./core-engine -e ./workers

# Validation
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic-core==2.46.4

# LangGraph (LLM Finance Approver workflow)
langgraph==1.2.5
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langchain-core==1.4.7

# Azure (optional — install workers[azure] extra for real Azure access)
# azure-servicebus==7.14.3
# azure-search-documents==12.0.0
# azure-ai-inference==1.0.0b9
# azure-identity==1.25.3
# azure-ai-contentsafety==1.0.0
# azure-ai-documentintelligence==1.0.2
# azure-storage-blob==12.30.0
# httpx==0.28.1
# openai==2.41.1

# Local workspace package — install from monorepo root
# pip install -e ./core-engine -e ./workers
